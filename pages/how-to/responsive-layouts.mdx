# Build responsive screen layouts

A guide on how to implement layout responsiveness leveraging on the available elements:

- `Grid`, `Container`, and the spacing system
- Breakpoints and viewports
- Responsive typography
- `Show` and `Hide` components
- `useMediaQuery` util function

Similar to https://mui.com/material-ui/guides/responsive-ui/
